%h3 个人订单一览

.alert.alert-info
  NOTICE: 这里列出了个人所有状态的订单

%table.table.table-condensed
  %tr.info
    %th 订单商品
    %th 数目
    %th 状态
    %th 创建日期
  - @orders.try(:each) do |order|
    %tr
      %td= link_to utf8_html(order.good.title), me_user_order_path(current_user, order)
      %td= order.quantity
      %td= order.show_state
      %td= order.created_at.to_s(:db)

%center= paginate @orders if @orders.present?
